# gpu-container-stack
One-shot bootstrap and management stack for multi-user GPU servers based on private Docker containers.  Each user gets an isolated SSH-accessible GPU container with rebuild, snapshot, storage accounting and GPU blame support.
